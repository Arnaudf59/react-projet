import React, {Fragment} from 'react';

const Articles = ({ article }) => {

    console.log(article) 

    return (
        <Fragment>
            <h2>
                Articles
            </h2>
        </Fragment>
    );
};

export default Articles;